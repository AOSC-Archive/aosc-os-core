# Note: zlib does not use a standard autotools configure script
./configure --prefix=/usr

# The following lines are copied from autobuild3/build/10-autotools.sh
make $MAKE_AFTER
make install BUILDROOT=$PKGDIR DESTDIR=$PKGDIR $MAKE_AFTER
