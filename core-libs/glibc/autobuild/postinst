#
# locale-gen utility is adapted from Arch Linux.
#
# However, in AOSC OS this will not be bundled with GLibc.
#

if [ -x /usr/bin/locale-gen ]; then
	echo "Regenerating Locale data..."
	locale-gen
else
	true
fi

#
# Historic flaw of AOSC OS3, comment out next 3 lines for derivative build.
#

ln -sv /usr/lib lib64
